//! This is a simple implementation of an AST (abstract syntax tree) in ziglang
//! for parsing numeric operations and storing variable.
const std = @import("std");

const print = std.debug.print;

pub fn main() void {
    print("Hola, {s}!\n", .{ "mundo" });
}
